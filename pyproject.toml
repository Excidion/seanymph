[project]
name = "seanymph"
version = "0.1.0"
description = "A matplotlib-style Python library for generating Mermaid diagrams"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
docs = [
    "zensical>=0.0.23",
]
